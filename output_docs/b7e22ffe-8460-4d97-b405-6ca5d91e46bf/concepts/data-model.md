# Geospatial indexing and proximity search visualization Documentation — Data model (narrative)

The top-20 most-referenced domain entities and how they relate. Different framing from the technical [`reference/data-model.md`](../reference/data-model.md) page — that one shows every entity and every field; this one is the narrative version designed for a non-technical reader: only the entities other entities depend on, only the relations between them.

_No domain entities detected. Either static analysis ran in a degraded mode or no ORM / schema patterns matched._
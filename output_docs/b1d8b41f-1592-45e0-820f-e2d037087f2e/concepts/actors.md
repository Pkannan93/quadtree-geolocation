# Geospatial indexing and proximity search via quadtree, with an interactive Swing visualization Documentation — Actors

Human or system actors that interact with the system, and the capabilities each one uses. 'Actor' is intentionally broad — a Scheduler, a Webhook, an Admin, an End User all count. Sourced from the business-semantics agent's domain rollup.

_No actors detected. Either the business-semantics agent ran in a degraded mode, or no auth / role / scheduler patterns were detected to seed the inference._
# Geospatial indexing and visualization using quadtrees (geolocation optimization / efficient spatial queries) Documentation — Deprecated

API surface that's marked `DEPRECATED` in source comments. These are user-facing promises about API stability — every entry here is something an integrator may be reading.

_No `DEPRECATED` markers found in source. This page also misses language-native deprecation annotations (Python's `@deprecated` from `typing_extensions`, Java's `@Deprecated`, TypeScript's `@deprecated` JSDoc tag) — those land in Sprint B's deprecated-annotation plugin._
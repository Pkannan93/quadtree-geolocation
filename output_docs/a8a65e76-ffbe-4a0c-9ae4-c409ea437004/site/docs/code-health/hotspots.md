# Geospatial indexing and visualization (quadtree-based location services) Documentation — Coupling hotspots

Modules ranked by **fan-in × fan-out** — the modules that both depend on a lot AND are depended on by a lot. Touching one of these is touching the seams between many subsystems.

| Score | Module | Fan-in (depended on by) | Fan-out (depends on) |
|---|---|---|---|
| — | _no graph_ | — | — |

_The module-dependency graph (alpha7 substrate) wasn't computed this run. Re-ingest with the alpha7 ingestion agent to populate this page; the projection is a pure read of the graph artifact._
# Geospatial indexing and visualization — quadtree-based proximity search over latitude/longitude points Documentation — Route auth matrix

Every route in the system, ordered by protection state (open routes first — they're the audit priority), then by path. Each row links back to the per-route reference page.

_No `route_details` in this run — the matrix is empty. Either no HTTP framework is detected, or static-analysis ran without the F2 enrichment pass._
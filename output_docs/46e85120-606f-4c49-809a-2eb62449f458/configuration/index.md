# Configuration — canvas-repo-sbb61m6j

What this service can be configured with — env vars (typed where a settings class declares them), feature flags, and CLI arguments.

## Pages

- [Environment](./environment.md) — **0 env vars** across **0 settings class(es)**
- [Feature flags](./feature-flags.md) — **0** flag(s)
- [CLI](./cli.md) — **0** flag(s)

## What each page is for

- **Environment** is the *typed* counterpart to `operations/environments.md`: settings-class fields with type / default / required / secret flags, joined with the env-call citations and any `.env.example` documented values.
- **Feature flags** lists every flag the codebase asks about, grouped by provider (LaunchDarkly / split.io / unleash / internal / env-gated). Each row carries every call site.
- **CLI** enumerates every command-line option and argument the service exposes, grouped by the owning command.


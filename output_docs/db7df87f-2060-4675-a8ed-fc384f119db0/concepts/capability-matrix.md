# System Documentation — Capability matrix

One row per capability, with its bounded context, the modules that implement it, and the routes it exposes (when an HTTP surface is involved). Routes deep-link into the per-route reference page so readers can pivot from the domain view to the technical contract.

## `uncategorised`

| Capability | Modules | Routes |
|---|---|---|
| **(no responsibility returned)** | `.` | — |
| **(no responsibility returned)** | `quadtree-graphic` | — |
| **(no responsibility returned)** | `quadtree-graphic/gradle/wrapper` | — |
| **(no responsibility returned)** | `quadtree-graphic/src/main/java/src` | — |
| **(no responsibility returned)** | `quadtree-graphic/src/main/java/src/quadtree` | — |
| **(no responsibility returned)** | `quadtree-graphic/src/main/java/src/quadtree/core` | — |


# Geospatial indexing and visualization (quadtree-based location services) Documentation — Capability matrix

One row per capability, with its bounded context, the modules that implement it, and the routes it exposes (when an HTTP surface is involved). Routes deep-link into the per-route reference page so readers can pivot from the domain view to the technical contract.

## `build tooling / project infrastructure`

| Capability | Modules | Routes |
|---|---|---|
| **wrapper** | `quadtree-graphic/gradle/wrapper` | — |


## `build tooling / project scaffolding`

| Capability | Modules | Routes |
|---|---|---|
| **Build infrastructure scaffolding for the `quadtree-graphic` Java project** | `quadtree-graphic` | — |


## `geospatial data structures / location-based services (quadtree-based spatial indexing)`

| Capability | Modules | Routes |
|---|---|---|
| **.** | `.` | — |


## `geospatial indexing / spatial data structures`

| Capability | Modules | Routes |
|---|---|---|
| **Provides the core quadtree spatial indexing implementation for geographic data** | `quadtree-graphic/src/main/java/src/quadtree/core` | — |


## `interactive visualization / demo ui for a quadtree spatial-indexing data structure`

| Capability | Modules | Routes |
|---|---|---|
| **src** | `quadtree-graphic/src/main/java/src` | — |


## `spatial data structure visualization / 2d graphics rendering`

| Capability | Modules | Routes |
|---|---|---|
| **quadtree** | `quadtree-graphic/src/main/java/src/quadtree` | — |

